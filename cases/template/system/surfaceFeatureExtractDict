FoamFile
{
    version     2.0;
    format      ascii;
    class       dictionary;
    object      surfaceFeatureExtractDict;
}

// 鼻・耳などの稜線を拾い、snappyHexMesh で局所細分化する
head.stl
{
    extractionMethod    extractFromSurface;
    includedAngle       120;
    writeObj            no;
}
